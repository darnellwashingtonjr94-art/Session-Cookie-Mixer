# Remove the "version: '3.8'" (or similar) line entirely. Start right at services:
services:
  # Your main app service
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    # ... any other configuration ...

  # Your e2e test service (referenced in your logs)
  test-e2e:
    build:
      context: .
      dockerfile: Dockerfile.e2e
    # ... any other configuration ...
